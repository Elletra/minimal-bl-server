// The first datablock in the list doesn't seem to be loading for some reason...
datablock CameraData(__DUMMY__) { _ = ""; };

datablock CameraData(FlyingCamera)
{
	// Data blocks *must* have at least one field.
	_ = "";
};
